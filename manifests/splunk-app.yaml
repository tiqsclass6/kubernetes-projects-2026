apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: splunk
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/tiqsclass6/kubernetes-projects-2026.git
    targetRevision: project-2
    path: manifests/splunk/base

  destination:
    server: https://kubernetes.default.svc
    namespace: splunk

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# kubectl apply -f splunk-app.yaml